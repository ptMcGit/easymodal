'use strict';

var monkey = require('../../index.js');
var assert = require('assert');

describe('something', function () {

    it('should do something', function (done) {
      assert.equal(monkey,5);
      done();
  });

});
